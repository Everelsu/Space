<Window x:Class="Space.MainProject"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Space"
        mc:Ignorable="d"
        Title="MainProject" Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#111827"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF3A4358"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="25"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#111827"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" 
                        Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="1" 
                        CornerRadius="8" 
                        Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1F2937"/>
                                <Setter TargetName="btnBorder" Property="BorderBrush" Value="#7B3FE4"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3A4358"/>
            <Setter Property="BorderBrush" Value="#1F2937"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

    </Window.Resources>

    <Grid Background="#111827">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="600*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Height="434" VerticalAlignment="Top">

            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="55"/>
                <RowDefinition Height="55"/>
                <RowDefinition Height="55"/>
                <RowDefinition Height="55"/>
                <RowDefinition Height="55"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.Row="0" HorizontalContentAlignment="Left" VerticalContentAlignment="Top">Space</Label>
            <Image Source="/Assets/Z.png" Height="30" VerticalAlignment="Top"/>

            <Button Grid.Row="1" Height="50" VerticalAlignment="Top" Click="Button_Click_2">Проекты</Button>
            <Button Grid.Row="2" Height="50" VerticalAlignment="Top">Работники</Button>
            <Button Grid.Row="3" Height="50" VerticalAlignment="Top">Команды</Button>
            <Button Grid.Row="4" Height="50" VerticalAlignment="Top">Отчеты</Button>

            <Button Background="Red" FontSize="10" Grid.Row="6" HorizontalAlignment="Left" VerticalAlignment="Center" Height="30" Width="60" Click="Button_Click_1">Выход</Button>

        </Grid>

        <Grid Grid.Column="1">
            <DataGrid x:Name="myDataGrid" AutoGenerateColumns="True" Height="370" VerticalAlignment="Bottom"/>
            <TextBox HorizontalAlignment="Center" VerticalAlignment="Top" Width="150" Margin="0,14,0,0" Height="20">🔍 Поиск</TextBox>
            <Button HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="10" Margin="0,11,10,0" Background="#374151" BorderBrush="#1F2937"> + Добавить проект</Button>
            <Label Margin="0,0,485,386">Проекты</Label>
        </Grid>

    </Grid>
</Window>
