<Window x:Class="Space.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SpaceZ"
        Height="650" Width="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#0F172A" CornerRadius="14" Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="24" ShadowDepth="0" Opacity="0.55"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ── Title bar ────────────────────────────────────────── -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="195"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Logo -->
                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="Space" FontSize="16" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"/>
                    <Image Source="/Assets/Z.png" Height="20" Margin="2,-2,0,0"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                </StackPanel>

                <!-- Window controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Margin="0,0,8,0">
                    <Button Content="─" Width="28" Height="28" FontSize="11"
                            Style="{StaticResource TitleBarBtnStyle}" Click="Minimize_Click"/>
                    <Button Content="✕" Width="28" Height="28" FontSize="11"
                            Style="{StaticResource TitleBarBtnStyle}" Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <!-- ── Body ─────────────────────────────────────────────── -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="195"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="#0B1120"
                        BorderBrush="#1E293B" BorderThickness="0,1,1,0">
                    <DockPanel>
                        <!-- User info -->
                        <Border DockPanel.Dock="Bottom" Padding="14,10"
                                BorderBrush="#1E293B" BorderThickness="0,1,0,0">
                            <StackPanel>
                                <TextBlock x:Name="SidebarRole"   Style="{StaticResource UserRoleStyle}"/>
                                <TextBlock x:Name="SidebarUsername" Style="{StaticResource UserNameStyle}"/>
                                <Button Content="Выйти" Margin="0,8,0,0" Height="28" FontSize="11"
                                        Style="{StaticResource NavBtnStyle}" Click="Exit_Click"/>
                            </StackPanel>
                        </Border>

                        <!-- Nav -->
                        <StackPanel Margin="8,8,8,0">
                            <TextBlock Text="НАВИГАЦИЯ" Style="{StaticResource NavSectionLabel}"/>
                            <Button x:Name="BtnProjects"  Content="📁  Проекты"   Style="{StaticResource NavBtnStyle}" Click="NavProjects_Click"/>
                            <Button x:Name="BtnEmployees" Content="👥  Сотрудники" Style="{StaticResource NavBtnStyle}" Click="NavEmployees_Click"/>
                            <Button x:Name="BtnTeams"     Content="🏢  Команды"   Style="{StaticResource NavBtnStyle}" Click="NavTeams_Click"/>
                            <Button x:Name="BtnTasks"     Content="✅  Задачи"    Style="{StaticResource NavBtnStyle}" Click="NavTasks_Click"/>
                            <Button x:Name="BtnReports"   Content="📊  Отчёты"   Style="{StaticResource NavBtnStyle}" Click="NavReports_Click"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- Page host -->
                <Border Grid.Column="1" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="0,1,0,0">
                    <ContentControl x:Name="PageHost"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
