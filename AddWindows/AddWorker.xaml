<Window x:Class="Space.AddWindows.AddWorker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Space.AddWindows"
        mc:Ignorable="d"
        Title="AddWorker" Height="600" Width="400">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="10"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#111827"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" 
                     Background="{TemplateBinding Background}" 
                     BorderBrush="{TemplateBinding BorderBrush}" 
                     BorderThickness="1" 
                     CornerRadius="8" 
                     Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1F2937"/>
                                <Setter TargetName="btnBorder" Property="BorderBrush" Value="#7B3FE4"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3A4358"/>
            <Setter Property="BorderBrush" Value="#1F2937"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="#FF3A4358"/>
            <Setter Property="BorderBrush" Value="#FF3A4358"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="DatePickerTextBox">
            <Setter Property="BorderBrush" Value="#FF3A4358"/>
            <Setter Property="Background" Value="#FF3A4358"/>
            <Setter Property="Foreground" Value="White"/>

        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FF3A4358"/>
            <Setter Property="BorderBrush" Value="#1F2937"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>


        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#374151"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

    </Window.Resources>

    <Grid Background="#111827">

        <Label FontSize="25" HorizontalAlignment="Center" Height="50" VerticalAlignment="Top">Добавление проекта</Label>

        <Label HorizontalAlignment="Left" Margin="100,54,0,508" Content="ФИ"/>
        <TextBox Width="200" Margin="100,76,100,490"/>

        <Button Width="70" Height="30" Margin="255,277,75,277">Cоздать</Button>
        <Button Width="50" Height="30" Margin="150,273,200,281">Отмена</Button>

    </Grid>
</Window>
